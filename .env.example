# 复制为 .env 后填写真实密钥；不要提交 .env。
SUNO_API_KEY=
DASHSCOPE_API_KEY=

# 本地服务监听端口。
PORT=3000

# 可被外部服务访问的回调基础地址；本地开发可使用公网隧道地址。
# Render 部署时会优先使用平台提供的 RENDER_EXTERNAL_URL。
PUBLIC_BASE_URL=http://localhost:3000
